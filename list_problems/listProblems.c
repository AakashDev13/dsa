#include<stdio.h>
#include<stdlib.h>
#include "list.h"

int main() {
    insertAtEnd(10);
    insertAtEnd(20);
    insertAtEnd(30);
    insertAtEnd(40);
    insertAtEnd(50);
    displayList();

    return 0;
}